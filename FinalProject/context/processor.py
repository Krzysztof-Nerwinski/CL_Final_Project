# from django.db.models import Q
# from timer.models import Timer
#
#
# def user_has_active_timer(request): #todo check later if needed
#     if Timer.objects.filter(Q(employee=request.user) & Q(is_active=True)):
#         return True
